#include "FLVSegmentInput.h"

//detect whether the next audio stream is ready or not
bool FLVSegmentInput::isNextRawAudioStreamReady(u32& audioTimestamp) {
    
}

//get next flv frame
SmartPtr<AccessUnit> FLVSegmentInput::getNextRawAudioFrame(u32 index) {

}
SmartPtr<AccessUnit> FLVSegmentInput::getNextRawVideoFrame(u32 index, u32 timestamp){

}
