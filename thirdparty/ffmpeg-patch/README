Here is a way to force rebuild after we modified ffmpeg/libavformat code

0. modify source code from libav-xxx/libavformat/flv.h, flvdec.c, etc
1. package the directory in .tar.gz file
2. copy the .tar.gz file to ~/Sites/ffmepg-patch
3. modify ./vlc/extras/tools/packages.mak to point it to 192.168.0.92/~wminghao/ffmpeg-patch/libav-HEAD-254c95c.tar.gz
4. run the build sh ./buildMobileVLCKit.sh -d -s -v from VLCKit

It's awkward
